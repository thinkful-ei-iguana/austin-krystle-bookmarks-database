CREATE TABLE bookmarks (
	id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	url TEXT NOT NULL,
	title TEXT NOT NULL,
	rating NUMERIC,
	description TEXT NOT NULL,
	tag TEXT DEFAULT 'Saved Site'
	);